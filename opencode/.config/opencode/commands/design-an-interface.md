---
description: Generate and compare radically different interface designs
---

Load and use the `design-an-interface` skill for this request.

First, invoke the skill:

```js
skill({ name: "design-an-interface" })
```

Then follow the skill instructions exactly.

<user-request>
$ARGUMENTS
</user-request>
